import { Text, View } from "react-native";
interface PhoneEntryProps {}

export const PhoneEntry: React.FC<PhoneEntryProps> = ({}) => {
  return (
    <View>
      <Text>PhoneEntry Page</Text>
    </View>
  );
};
