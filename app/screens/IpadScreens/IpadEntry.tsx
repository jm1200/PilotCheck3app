import { Text, View } from "react-native";

interface IpadEntryProps {}

export const IpadEntry: React.FC<IpadEntryProps> = ({}) => {
  return (
    <View>
      <Text>IpadEntry Page</Text>
    </View>
  );
};
